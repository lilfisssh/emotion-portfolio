---
target: emotion-portfolio App (Emotional Weather)
total_score: 30
p0_count: 0
p1_count: 3
timestamp: 2026-07-20T16-01-12Z
slug: src-app-tsx
---
# Critique — Emotional Weather (emotion-portfolio, App root)

Method: dual-agent (A: explore/design-review · B: explore/detector CLI). Browser evidence gathered in parent context (shared single-browser constraint via WebBridge). Target: src/App.tsx + components.

## Heuristics (Nielsen, 0–4) — Total 30/40 GOOD

| # | Heuristic | Score | Key issue |
|---|-----------|-------|-----------|
| 1 | Visibility of system status | 4 | dot+text+Calibrating countdown+WS reconnecting…; status label text is ink-3 (2.4:1) |
| 2 | Match system / real world | 3 | plain-English mood words; RMSSD/Skin Conduct./0.6×HR+0.4×GSR assume technical reader |
| 3 | User control & freedom | 3 | connect/disconnect/mock reversible; select disabled-while-connected unexplained |
| 4 | Consistency & standards | 3 | "02" eyebrow with no "01"; lavender stripe breaks no-boxes rule; switch=button in label |
| 5 | Error prevention | 2 | connect disabled w/o ports ✓; /api/connect failures silently swallowed |
| 6 | Recognition rather than recall | 4 | all state visible; mood word + numbers + waveforms redundant-coded |
| 7 | Flexibility & efficiency | 2 | no keyboard shortcuts, no ?mock deep link, mock re-enabled every visit |
| 8 | Aesthetic & minimalist | 4 | hairline rows, borderless plane, zero cards |
| 9 | Error recovery | 2 | WS auto-reconnect good; hardware errors have no visible message/retry |
| 10 | Help & documentation | 3 | real method section + honest note; plane itself unlabeled |

## Anti-patterns verdict

LLM assessment: PASS overall — motion is semantic (arousal drives letter scatter, blob saturation, halo pulse), zero cards, no gradient text, no glassmorphism abuse. Two canonical tells: (1) side-stripe 2px lavender border-left on "Honest note" callout (App.tsx); (2) orphaned numbered eyebrow "02 — METHOD" with no 01. Not flagged: committed font identity (Archivo Black/Fraunces/Space Grotesk/Inter/JetBrains Mono) — identity preservation.

Deterministic scan (detect.mjs): 1 finding — overused-font (Fraunces) at index.html:9. Judged FALSE POSITIVE in context: fonts are the committed v4 brand identity. Everything else clean.

## Fidelity to design-brief v4

Faithful param-by-param (porcelain/ink, 4 blobs blur 80px quadrant pastels, title clamp 44–80px, single Fraunces italic line, left-5/right-7, particle-letter math, plane fadeLen 0.12/pad 64/τ0.9s/trail 60ms-0.6px-60s/α0.5-flow/3px/τ1.2s, r9 core + 13×13 halo step 0.17 pulse sin(ph1×2−rn×4) R=46+5sin, waveforms midpoint-bezier 1.5px α0.55). Deviations: stale EmotionPlane docstring (promises quadrant wall labels, none drawn); dead CSS (.panel/.outline-word/.glow-breathe); stale "象限中文判定" comment; Waveform x-divisor hardcoded 59 vs 120-point series.

## Cognitive load

FAIL: unlabeled plane semantics (dot position meaning lives only in method section below fold). Borderline: left column 5–6 chunks; mood word vs plane compete (mitigated by shared color).

## Emotional journey

Real peak (word flip → letters scatter + sky re-tints); honest close (footer HRV disclaimer); calibration reassurance ×2 (SENSING + sweep). Broken note: offline no-sample renders giant coral "—" (emptiest state in most excited color).

## Persona red flags

- Alex: select outline-none + no focus substitute; no shortcuts; mock needs mouse each visit.
- Sam: --ink-3 ≈2.4:1 across ~10 elements; EmotionPlane/Waveform canvases unlabeled; switch nameless; live regions undefined; quadrant inks pass only as large text.
- Casey (360px): Connect h-8=32px, switch 20×36px, rigid single-row bar overflows/clips at ~360px.

## Priority issues

1. [P1] Tertiary text ink-3 2.4:1 contrast → raise to rgba(20,20,18,0.6) (~5:1).
2. [P1] prefers-reduced-motion ignored by all JS canvas loops (MoodField/EmotionPlane/Waveform) + letter jitter → shared reduced-motion gate; freeze decorative layers, keep data updates.
3. [P1] Invisible keyboard focus (select outline-none, no global :focus-visible) → global ink focus ring.
4. [P2] Control-bar touch targets 20–32px + 360px overflow → min 40px controls, allow wrap/collapse.
5. [P2] Canvas a11y + switch naming + aria-live policy.
P3: honest-note stripe, orphaned 02, offline coral "—", dead CSS, Waveform /59.

## Strengths

1. Motion as data not decoration (parametric per brief).
2. Compositional discipline: zero cards, hairlines, borderless plane dissolving into field.
3. Honest states: calibration countdown, SENSING, HRV honest note, footer disclaimer.

## Minor observations

Mock status dot #3A6FC0 collides with "low" quadrant ink; grain+80px-blur compositing stack heavy on mid phones; refreshPorts silently swaps vanished port; post() swallows fetch errors (no UI).

## Questions to consider

1. Calm (most common/healthy state) yields the quietest page — should calm earn its own richness?
2. Does the piece teach the circumplex in 8 seconds without scrolling to "How it works" (quadrant labels)?
3. Should first paint default to mock performing beautifully for hardware-less portfolio reviewers?
