# 设计简报 · Arduino 情绪检测实时展示页

> **v4（当前生效）：「情绪气候 / Emotional Weather」浅色前卫方向**——瓷白 `#FAFAF7` 底 + 墨色 `#141412`；整页粉彩活体色场（4 个漂移光团 blur 80px，象限映射：兴奋珊瑚 `#FFA08C` / 焦虑薰衣草 `#BEA0EB` / 低落粉末蓝 `#A0BEE6` / 平静薄荷 `#A0E1C8`）；Archivo Black 巨型标题；情绪判定词与标题共用象限饱和文字色（`#E0573A`/`#7A4FD0`/`#3A6FC0`/`#1F9E78`）。全英文 UI。v1–v3 均已否决，以下仅存档。

## v4 最终实现参数（2026-07-20 定稿，与代码一致）

**字体栈**：Archivo Black（巨型标题 + 心情判定词）/ Fraunces italic（唯一衬线点缀句 "your body is quietly repainting this sky."）/ Space Grotesk（display）/ Inter（body）/ JetBrains Mono tabular-nums（读数）。Google Fonts 加载 Fraunces 全可变范围 `ital,opsz,wght@0/1,9..144,100..900`。

**首屏布局（一眼全览）**：左栏 5 列 = eyebrow（EMOTIONAL WEATHER — REAL-TIME BIO-SIGNAL + LIVE 灯）→ Archivo Black 标题（EMOTIONAL 墨 / WEATHER 象限色，clamp 44–80px）→ 衬线点缀句 → 传感器说明行 → RIGHT NOW 超大判定词（STEADY/EXCITED/TENSE/LOW/CALM，校准中 SENSING）→ 三行信号（标签 + mono 大数 + 波形，hairline 分隔）→ VALENCE/AROUSAL 行；右栏 7 列 = 无框情绪平面，与色场融为一体。

**心情判定词 = 情绪粒子字**：Archivo Black 大写，每字母独立位移+旋转，幅度随唤醒度：位移 1.5+|a|×5 px，旋转 ±(0.6+|a|×2.4)°，1.2s ease 过渡；换词时逐字母浮入（45ms stagger，0.7s cubic-bezier(0.22,1,0.36,1)，blur→清晰）。平静时齐整、激动时晃动——字本身表达情绪强度。

**情绪平面（无边框）**：十字轴两端渐隐 fadeLen 0.12，轴线上限 alpha 0.55；内边距 pad=64（防光晕裁切）；点平滑 τ0.9s。**拖尾** = 光标平滑位置历史（60ms/0.6px 采样，60s 窗）逐段绘制，alpha 0.5×流动波（accS×0.045 − now/850），线宽 3px，颜色以 τ1.2s 向象限粉彩插值。**光标** = 粉彩柔芯 r9（中心 alpha 1）+ 13×13 矩形网格点阵光晕（step 0.17，径向扩散脉冲 sin(ph1×2 − rn×4)，半径 R=46+5sin）。

**波形**：中点贝塞尔平滑，1.5px，alpha 0.55，象限色。

**工程**：后端 8100 常驻；`server/index.js` 检测 EADDRINUSE 静默复用（http server 与 WSS 均挂 error 处理，exit 0 不拖垮 dev.mjs 同启的 Vite）。旧版 serial-dashboard 与 codes-副本 已删除。

---

# （v1–v3 存档 · 已否决）

> 气质定位：**深夜实验室里一台有体温的仪器** —— 未来科技感的深色仪器骨架（精密读数、hairline 网格、单色信号光），叠加温暖人文感的呼吸节律（琥珀暖光、有机平滑插值、心跳同频的微脉冲）。克制、可信、活着。
> 页面语言：中文为主，全站深色。低饱和。所有图表 SVG/Canvas 手绘。

---

## 1. 配色（全部低饱和，给 hex）

### 1.1 基础表面阶梯（层级靠明度阶梯 + 1px hairline，不用投影）

| 用途 | hex | 说明 |
|---|---|---|
| 页面底色 canvas | `#0A0B0D` | 近黑带一丝冷调，仪器表盘的"暗室" |
| 一级面板 surface-1 | `#121316` | 传感器面板、控制条底色 |
| 二级面板 surface-2 | `#17181C` | hover / 抬起态（仅明度变化） |
| 细边框 hairline | `#262A31` | 全部 1px，替代阴影与卡片边框 |
| 强边框 hairline-strong | `#343A44` | 输入框 focus、象限平面外框 |

### 1.2 文字阶梯

| 用途 | hex |
|---|---|
| 主文字（标题、大读数） | `#F7F8F8` |
| 次级文字（正文、标签） | `#B8BCC4` |
| 弱化文字（注释、单位、时间戳） | `#69707A` |

### 1.3 信号主色（全站唯一高彩度色，稀缺使用）

| 用途 | hex | 说明 |
|---|---|---|
| **信号琥珀 signal** | `#FF9E40` | 当前情绪点、实时波形主线、主 CTA、focus ring。暖橙琥珀——心跳监测仪的信号灯色，科技感与体温感的交汇点 |
| 信号光晕 | `#FF9E40` 10%–36% 透明度分层 | 只用于当前点光晕与拖尾衰减，不做装饰渐变 |

### 1.4 四象限分区色（极低透明度铺底，8%–10% opacity，色相互补、饱和度统一压低）

| 象限 | 语义 | 底色 hex | 依据 |
|---|---|---|---|
| 右上（高唤醒+积极）兴奋/愉悦 | `#D9A24A` | 暖金，与信号琥珀同族，"上扬的能量" |
| 左上（高唤醒+消极）紧张/焦虑 | `#D85C46` | 哑珊瑚红，警示但不刺眼 |
| 左下（低唤醒+消极）低落/疲倦 | `#5F7FA8` | 灰蓝，下沉、冷、慢 |
| 右下（低唤醒+积极）放松/平静 | `#6FA88F` | 灰绿苔色，呼吸、恢复 |

四色按同一饱和度/明度阶梯推导，确保任意两象限相邻不打架。象限标签文字用对应色 90% 明度版本。

### 1.5 当前点 & 拖尾

| 元素 | 规格 |
|---|---|
| 当前点核心 | `#FFB86B`（signal 提亮一档），实心圆 r=7px |
| 当前点光晕 | 两层 box-shadow/glow：`0 0 16px rgba(255,158,64,.18)` / `0 0 32px rgba(255,158,64,.10)`，3s 呼吸脉冲 |
| 拖尾轨迹 | 最近 60s 轨迹折线，stroke `#FF9E40`，沿时间轴 opacity 从 0.55 衰减到 0，线宽 2px→0.5px 同步衰减；采样点不画圆点，只画线 |

### 1.6 连接状态灯（两态 + 校准态，三色区分）

| 状态 | hex | 表现 |
|---|---|---|
| 串口实时（serial） | `#FF9E40` 琥珀 | 常亮小圆点 + 慢脉冲——"活体数据" |
| 模拟演示（mock） | `#5FA8C7` 灰蓝 | 常亮小圆点，无脉冲——"回放/演示"，冷静区分于活体 |
| 基线校准中（前 30s） | `#D9A24A` 暖金 | 1.2s 闪烁 + 旁注"校准中 xs" |
| 断开/未连接 | `#69707A` 灰 | 静态空心圆 |

---

## 2. 字体方案

### 2.1 字体栈（中文必须显式声明，禁止纯拉丁显示字体渲染中文标题）

```css
--font-display: "Inter", "SF Pro Display", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-body:    "Inter", "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-mono:    "JetBrains Mono", "SF Mono", "Menlo", "PingFang SC", "Microsoft YaHei", monospace;
```

- Google Fonts 加载 Inter + JetBrains Mono，`font-display: swap`；中文回落系统字体。
- 所有数字读数加 `font-variant-numeric: tabular-nums`，实时刷新时数字不跳动。

### 2.2 字重字号搭配

| 角色 | 字体 | 字号 | 字重 | 字距 | 说明 |
|---|---|---|---|---|---|
| Hero 主标题 | display | 56–64px | 600 | -0.03em（仅拉丁）| 负字距是拉丁排版手法，中文部分字距保持 0 |
| 章节 eyebrow 编号 | mono | 13px | 500 | +0.06em | `01 / SIGNAL SOURCE` 式编号标签，建立仪器文档感 |
| 章节标题 | display | 32–40px | 600 | -0.02em | |
| **大数字读数（HR / GSR / RMSSD）** | mono | 48–64px | 500 | 0 | tabular-nums；单位缩小到 16px 灰字跟在基线上 |
| 象限标签（兴奋/焦虑/放松/低落） | body 中文 | 14px | 500 | +0.1em | 中文加宽字距代替斜体做强调 |
| 正文 / 技术说明 | body | 15–16px | 400 | 0 | 行高 1.6 |
| 时间戳 / 坐标轴刻度 | mono | 12px | 400 | 0 | `#69707A` |

**禁忌重申：全站无斜体中文；中文强调用字重、颜色、字距，不用 italic。**

---

## 3. 布局（单页五区，顺序固定）

最大内容宽 1280px，12 列栅格，区间纵向间距 96px，面板内边距 24px。深色 canvas 本身就是留白——分区靠表面阶梯与 hairline 分隔，不靠大缝隙。

1. **Hero（首屏上段，约 55vh）**：左 7 列文字栈（eyebrow 编号 `01 / EMOTION INSTRUMENT` + 主标题 + 一句项目说明 + 状态摘要行），右 5 列小型实时摘要监视窗（HR 大读数 + 迷你波形，surface-1 面板，7s 微浮动）。背景叠 64px 工程网格线，38s 线性缓慢平移，营造"测量台面"感。
2. **连接控制条（sticky，高 56px）**：左=状态灯 + 模式文字（串口实时 COMx / 模拟演示 / 校准中倒计时）；中=串口下拉 + 连接/断开按钮；右=模拟模式开关。全程吸顶，它是"仪器的电源面板"。
3. **情绪平面主视觉（核心，约 90vh）**：占满内容宽，surface-1 大面板，内部正方形 Canvas 情绪平面 + 右侧窄栏（240px）实时读数列（Valence / Arousal 数值 + 象限判定中文大标签）。平面是主角，其他元素不许抢视觉。
4. **传感器面板**：3 列（桌面）× 各一路信号：HR(bpm) / GSR(μS) / RMSSD(ms)。每列 = mono 大读数 + 60s 迷你波形（Canvas 手绘折线，signal 色 2px，无填充渐变）。三列之间用 hairline 分隔，不做卡片套卡片。
5. **技术说明**：双列。左列=映射方法（Arousal = HR 0.6/GSR 0.4 加权，Valence = RMSSD 弱代理——**必须显著标注"估算"**）；右列=系统架构简述与协议契约。 eyebrow 编号延续（`05 / METHOD`）。

移动端：全部单栏，情绪平面保持正方形占满屏宽，读数列移到平面下方。

---

## 4. 情绪平面视觉语言（本页记忆点所在）

- **象限分界**：不用实墙。两轴十字 hairline（`#343A44` 1px）居中相交，四象限以各自分区色 8% 透明度铺底，色相过渡靠低透明度自然融合，不画渐变。
- **坐标轴处理**：轴不穿出平面边缘，端点留 12px 呼吸空间；刻度只有 -1 / 0 / +1 三档，mono 12px 灰字；轴名"欢愉度 Valence →""唤醒度 Arousal ↑"置于轴端正中，中文在前拉丁在后。
- **当前点形态**：三层结构——7px 实心琥珀核心 + 双层静态光晕 + 一圈 3s 呼吸脉冲外环（见 §6）。点不贴标签，象限判定文字显示在右栏，平面保持干净。
- **拖尾轨迹**：60s 衰减折线（§1.5），轨迹是"情绪刚刚走过的路"，是本页最有叙事性的元素——宁可点小一点，也要让轨迹可读。
- **"基线校准中"状态**：点冻结在圆心，外围一个虚线圆环以 12s 匀速扫过（stroke-dasharray 动画），平面叠加 12px mono 倒计时"基线校准中 18s"；波形区照常实时滚动（信号是真的，只是映射未就绪）。
- **平面外框**：1px hairline-strong，四角带 12px 直角刻度标记（像示波器屏幕的边角标线），强化仪器感。

---

## 5. 动效（只保留有意义的，每项附"为什么"）

| 动效 | 参数 | 为什么存在 |
|---|---|---|
| 当前点平滑插值 | rAF 60fps，位置 lerp 系数 ~0.12（约 300ms 追平） | WS 每秒 1 帧，不插值点会"跳台阶"；插值让运动连续，传达"活体测量"而非刷新快照。**核心动效** |
| 当前点呼吸光晕 | 3s ease-in-out 无限循环，glow 透明度 .18→.28 | 心跳节律的抽象化——让页面与用户心率同频，是"温暖人文感"的主要载体 |
| 拖尾实时绘制 | 60fps Canvas 重绘，新段淡入 200ms | 数据流动的可视化，没有它平面就是静态散点图 |
| 波形滚动 | 60fps，从右向左推，新样本从右缘进入 | 仪器示波器的本能语言，实时性的直接证据 |
| 校准扫环 | 12s linear 无限，stroke-dashoffset | 告知"系统在工作但尚未就绪"，替代无意义的 loading spinner |
| 网格背景平移 | 38s linear 无限，64px 步进 | 环境层微动，让深色底不"死"，幅度小到不分散注意力 |
| Hero 监视窗浮动 | 7s ease-in-out，±10px | 单一的"悬浮仪器"隐喻，全页仅此一处浮动元素 |
| 入场 reveal | 0.56s cubic-bezier(0.22,1,0.36,1)，兄弟元素 70ms 阶梯延迟 | 仅页面加载时一次，建立次序感；滚动不触发 |
| 数值变化 | mono 数字直接替换，无滚动动画；变化超过阈值时颜色短暂提亮 400ms | 数字跳变本身就是信息；tabulate-nums 保证宽度稳定 |

**全局**：`prefers-reduced-motion` 时关闭呼吸、浮动、网格平移，保留数据插值与波形（它们是功能不是装饰）。

---

## 6. 禁忌清单（明确排除）

- ❌ 蓝紫渐变、任何装饰性渐变填充（波形下面积、按钮、背景一律不许）
- ❌ 卡片套卡片：分区靠表面阶梯 + hairline + 留白，最多"面板一层"
- ❌ emoji 图标、Font Awesome 彩色圆角方块图标；图标用 1.5px 描边线性 SVG 或纯文字
- ❌ 无意义 hover 缩放（scale-105）、图片 hover 旋转、滚动触发 fade-in-up
- ❌ loading spinner（校准态用扫环，§5）
- ❌ 走马灯 marquee、视差滚动
- ❌ 居中 Hero + 双 CTA + 三等分特性卡的模板结构
- ❌ 斜体中文；纯拉丁显示字体渲染中文标题
- ❌ 投影堆叠（dark 模式下层级靠明度阶梯，不用 box-shadow 浮雕）
- ❌ 第二个高彩度强调色——琥珀是唯一信号色，状态色全部压低饱和
- ❌ 玻璃拟态大面积滥用（仅控制条允许 backdrop-blur，因为它悬浮在实时数据之上）
- ❌ "modern and elegant""clean and intuitive"式空洞文案；技术说明必须写真实参数（0.6/0.4 加权、30s 基线、5s 滑窗、valence 为估算）

---

## 7. 设计依据摘要（决策逻辑）

- **深色仪器骨架**：数据来自真实传感器，页面应像一台可信的测量设备——近黑底色、mono 读数、hairline 网格、编号章节，全部服务于"这台仪器说的是真的"。
- **琥珀单信号色**：心跳监护与示波器的经典信号灯色，暖调天然携带体温感，避免冷蓝科技模板；严格稀缺使用（点、波形、CTA、focus），多一处都贬值。
- **呼吸与插值**：情绪数据是慢的、连续的、生物性的——动效语言与之同构（3s 呼吸 ≈ 静息心率周期），拒绝一切机械跳变。
- **象限四色低饱和铺底**：Russell 模型的四象限需要可读的语义分区，但饱和分区色会压过数据本身，故统一压到 8% 透明度，让琥珀点永远是最亮的元素。
